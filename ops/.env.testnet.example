# Testnet deployment environment variables
# Copy to .env.testnet and fill in your values:
#   cp ops/.env.testnet.example ops/.env.testnet

# Base Sepolia RPC (default). For OP Sepolia use https://sepolia.optimism.io
RPC_URL=https://sepolia.base.org

# Postgres connection string (Railway, Neon, Supabase, or self-hosted)
DATABASE_URL=postgres://user:pass@host:5432/cortex

# Public API URL used by solver and smoke tests after deployment.
API_URL=http://localhost:3001
API_PORT=3001

# Private keys — NEVER commit real keys. Use a dedicated testnet wallet.
DEPLOYER_KEY=0x<your-deployer-private-key>
SOLVER_PRIVATE_KEY=0x<your-solver-private-key>
AGENT_PRIVATE_KEY=0x<your-agent-private-key>
